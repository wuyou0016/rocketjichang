# Rocket机场 Final UI Asset Pack v2

This pack is derived from the user's selected FINAL LIGHT / FINAL DARK reference images.
It is intentionally split into two categories:

1. `*-reference.png`: exact visual slices from the final reference. Use for visual comparison and,
   where the slice is a pure art layer, as an art layer.
2. micro component PNGs: isolated/cropped visual elements extracted from the reference.

IMPORTANT:
- Do NOT replace SEO text, provider names, scores, prices, ranking values, or links with images.
- Keep all semantic text as HTML/Astro text.
- Use image assets only for visual decoration/branding.
- Preserve aspect ratio. Never use `object-fit: cover` on these assets.
- The two reference files are the source of truth:
  rocketjichang_FINAL_DARK_768x1024.png
  rocketjichang_FINAL_LIGHT_768x1024.png

The final visual target is 768x1024 reference composition. Scale proportionally for desktop.
